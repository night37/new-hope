import { Routes } from '@angular/router';
import { HomeComponent } from './home/home.component'; // Importez le composant ici !

export const routes: Routes = [{ path: 'home', component: HomeComponent }];
