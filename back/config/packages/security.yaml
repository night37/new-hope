security:
  # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
  password_hashers:
    Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: "auto"
  # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
  providers:
    app_user_provider:
      entity:
        class: App\Entity\User
        property: email
    app_structure_provider:
      entity:
        class: App\Entity\Structure
        property: email
    app_combined_provider:
      chain:
        providers: ['app_user_provider', 'app_structure_provider']
  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false
    login:
      pattern: ^/api/login
      stateless: true
      json_login:
        check_path: /api/login_check
        provider: app_combined_provider
        username_path: email
        password_path: password
        success_handler: lexik_jwt_authentication.handler.authentication_success
        failure_handler: lexik_jwt_authentication.handler.authentication_failure

    api:
      pattern: ^/api
      stateless: true
      provider: app_combined_provider
      jwt: ~

    main:
      lazy: true
      provider: app_combined_provider
      form_login:
        username_parameter: _username
        password_parameter: _password
        login_path: app_login
        check_path: app_login
        enable_csrf: true
        target_path_parameter: _target_path
      entry_point: form_login
      logout:
        path: app_logout
        target: app_login


      remember_me:
        secret: "%kernel.secret%"
        lifetime: 604800
        path: /
        # by default, the feature is enabled by checking a checkbox in the
        # login form, uncomment the following line to always enable it.
        #always_remember_me: true
      access_denied_handler: App\Security\AccessDeniedHandler

      # activate different ways to authenticate
      # https://symfony.com/doc/current/security.html#the-firewall

      # https://symfony.com/doc/current/security/impersonating_user.html
      # switch_user: true

  # Easy way to control access for large sections of your site
  # Note: Only the *first* access control that matches will be used
  access_control:
    - { path: ^/user/\d+/edit, roles: [ROLE_ADMIN, ROLE_USER] }
    - { path: ^/user, roles: [ROLE_ADMIN] }
    - { path: ^/animal, roles: [ROLE_ADMIN, ROLE_USER] }
    - { path: ^/structure, roles: [ROLE_ADMIN] }
    - { path: ^/structure/\d+/edit, roles: [ROLE_ADMIN, ROLE_STRUCTURE] }
    - { path: ^/api/backoffice, roles: IS_AUTHENTICATED_FULLY }
when@test:
  security:
    password_hashers:
      # By default, password hashers are resource intensive and take time. This is
      # important to generate secure password hashes. In tests however, secure hashes
      # are not important, waste resources and increase test times. The following
      # reduces the work factor to the lowest possible values.
      Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
        algorithm: auto
        cost: 4 # Lowest possible value for bcrypt
        time_cost: 3 # Lowest possible value for argon
        memory_cost: 10 # Lowest possible value for argon
